version: '3.8'

services:
  backend:
    build: ./services/backend
    ports:
      - "8000:8000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - pong-network
    stdin_open: true
    tty: true

networks:
  pong-network:
    driver: bridge
